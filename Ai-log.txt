i have this code to connect with supabase import { createClient } from '@supabase/supabase-js';
const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);
const { data, error } = await supabase.from('profiles').select('*');
console.log(data); but it wont run

how to commit in replit

create me a register and login page with flutter using supabase_flutter auth.
4. Handle errors with SnackBar and navigate to HomePage on success.

the register should have username email and password, and the log in only with email and password, make a beautiful design which is modern and minimalistic based on current design trends and based on best practices

i have done this two scripts for the database in supabase create extension if not exists "uuid-ossp";

create table if not exists public.profiles (
  id uuid primary key default uuid_generate_v4(),
  user_id uuid unique not null,
  email text unique,
  full_name text,
  avatar_url text,
  created_at timestamp with time zone default timezone('utc', now()),
  updated_at timestamp with time zone default timezone('utc', now())
);

create unique index if not exists profiles_user_id_idx on public.profiles(user_id);

alter table public.profiles enable row level security;

-- Allow users to view their own profile
create policy "Users can view their own profile" 
  on public.profiles for select 
  using (auth.uid() = user_id);

-- Allow users to update their own profile
create policy "Users can update their own profile" 
  on public.profiles for update 
  using (auth.uid() = user_id);

what should i add/change in the sql editor

the supabase url and anon key are in hte .env SUPABASE_URL and SUPABASE_ANON_KEY 

Generate a Flutter page that connects to a Supabase table called "tasks".
7. The page should:
8. - fetch all tasks from the table
9. - display them in a ListView
10. - allow adding a new task with a TextField and Button
11. - mark tasks as done by tapping on them
12. - update the Supabase table accordingly
14. Ngjiteni kodin në një skedar të ri, p.sh.:
15. lib/pages/tasks_page.dart

it has a table in it tasks with id uuid uuid_generate_v4() Identifikues unik
title text — Përshkrimi i detyrës
is_done boolean false Statusi i përfundimit

use that table for this assigment


make the login sql query for the login to create the users table


